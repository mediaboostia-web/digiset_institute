import { PageStub } from "@/components/page-stub";

export default function MentionsLegalesPage() {
  return (
    <PageStub
      title="Mentions légales"
      route="/mentions-legales"
      sections={["Contenu texte structuré (éditeur du site, hébergeur, RCCM)"]}
    />
  );
}
